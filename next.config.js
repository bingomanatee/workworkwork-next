/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  /* config options here */
  resolve: { extensions: ['', '.js', '.jsx','.css'], 'root': __dirname },
};

module.exports = nextConfig;
